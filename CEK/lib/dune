(include_subdirs unqualified)

(library
  (name lib)
  (modules
    compiler debug
    intro parser
    parse_cek anf CEK
    parse_krivine krivine)
  (libraries sexplib)
  (preprocess (pps ppx_sexp_conv)))
